Function takes a string and size n of the string. 
For n char in the string, prints ASCII value of each char, separated by " " (space), 
INCLUDING the NULL terminator.

The program takes in arguments strings to print.